"""\n策略 C: 區間震盪策略 (Range-Bound Oscillation)\n\n核心邏輯:\n1. 確認盤整: ADX < 25\n2. 超賣進場: 價格觸及 BB 下軌 + RSI < 30 + 成交量萎縮\n3. 超買進場: 價格觸及 BB 上軌 + RSI > 70 + 成交量萎縮\n4. 出場: 價格回到 BB 中軌 或 ATR 止損\n\n改進點:\n- 多重過濾機制 (比簡單 BB 反轉更嚴謹)\n- 支持 ATR 動態止損\n- 自動風報比調整\n"""\n\nimport pandas as pd\nimport numpy as np\nfrom typing import Dict, Optional\n\n\nclass RangeBoundStrategy:\n    \"\"\"\n    區間震盪策略\n    \n    適用場景: 盤整市、震盪市\n    避免場景: 強勢趨勢市 (ADX > 30)\n    \"\"\"\n    \n    def __init__(\n        self,\n        adx_threshold: float = 25,\n        rsi_oversold: float = 30,\n        rsi_overbought: float = 70,\n        volume_ma_period: int = 20,\n        volume_threshold: float = 0.8,\n        use_atr_stops: bool = True,\n        atr_multiplier: float = 2.0,\n        fixed_stop_pct: float = 0.02,\n        target_rr: float = 2.0\n    ):\n        self.adx_threshold = adx_threshold\n        self.rsi_oversold = rsi_oversold\n        self.rsi_overbought = rsi_overbought\n        self.volume_ma_period = volume_ma_period\n        self.volume_threshold = volume_threshold\n        self.use_atr_stops = use_atr_stops\n        self.atr_multiplier = atr_multiplier\n        self.fixed_stop_pct = fixed_stop_pct\n        self.target_rr = target_rr\n        \n    def add_indicators(self, df: pd.DataFrame) -> pd.DataFrame:\n        \"\"\"\u8a08算所需指標\"\"\"\n        df = df.copy()\n        \n        # ATR (Average True Range)\n        high_low = df['high'] - df['low']\n        high_close = np.abs(df['high'] - df['close'].shift())\n        low_close = np.abs(df['low'] - df['close'].shift())\n        ranges = pd.concat([high_low, high_close, low_close], axis=1)\n        true_range = np.max(ranges, axis=1)\n        df['atr'] = true_range.rolling(14).mean()\n        \n        # Bollinger Bands (20, 2)\n        df['bb_mid'] = df['close'].rolling(20).mean()\n        bb_std = df['close'].rolling(20).std()\n        df['bb_upper'] = df['bb_mid'] + (2 * bb_std)\n        df['bb_lower'] = df['bb_mid'] - (2 * bb_std)\n        df['bb_width'] = (df['bb_upper'] - df['bb_lower']) / df['bb_mid']\n        \n        # RSI (14)\n        delta = df['close'].diff()\n        gain = (delta.where(delta > 0, 0)).rolling(14).mean()\n        loss = (-delta.where(delta < 0, 0)).rolling(14).mean()\n        rs = gain / loss\n        df['rsi'] = 100 - (100 / (1 + rs))\n        \n        # ADX (14)\n        plus_dm = df['high'].diff()\n        minus_dm = -df['low'].diff()\n        plus_dm[plus_dm < 0] = 0\n        minus_dm[minus_dm < 0] = 0\n        \n        tr_sum = true_range.rolling(14).sum()\n        plus_di = 100 * (plus_dm.rolling(14).sum() / tr_sum)\n        minus_di = 100 * (minus_dm.rolling(14).sum() / tr_sum)\n        \n        dx = (np.abs(plus_di - minus_di) / (plus_di + minus_di)) * 100\n        df['adx'] = dx.rolling(14).mean()\n        \n        # Volume MA\n        df['volume_ma'] = df['volume'].rolling(self.volume_ma_period).mean()\n        \n        return df\n    \n    def generate_signal(self, df: pd.DataFrame) -> Dict:\n        \"\"\"\n        生成交易信號\n        \n        Returns:\n            {\n                'signal': 'buy' | 'sell' | 'hold',\n                'stop_loss': float,\n                'take_profit': float,\n                'entry': float,\n                'adx': float,\n                'rsi': float\n            }\n        \"\"\"\n        if df.empty:\n            return {'signal': 'hold', 'stop_loss': None, 'take_profit': None}\n        \n        df = self.add_indicators(df)\n        row = df.iloc[-1]\n        \n        # 檢查盤整狀態\n        if pd.isna(row['adx']) or row['adx'] >= self.adx_threshold:\n            return {'signal': 'hold', 'stop_loss': None, 'take_profit': None, 'reason': 'ADX 過高 (趨勢市)'}\n        \n        if pd.isna(row['bb_width']) or row['bb_width'] < 0.01:\n            return {'signal': 'hold', 'stop_loss': None, 'take_profit': None, 'reason': 'BB 寬度過窄'}\n        \n        current_price = row['close']\n        atr = row['atr']\n        volume_contracted = row['volume'] < (row['volume_ma'] * self.volume_threshold)\n        \n        # === 做多條件: 超賣反彈 ===\n        if (row['close'] <= row['bb_lower'] and \n            row['rsi'] < self.rsi_oversold and \n            volume_contracted):\n            \n            # 計算止損\n            if self.use_atr_stops:\n                stop_loss = current_price - (atr * self.atr_multiplier)\n            else:\n                stop_loss = current_price * (1 - self.fixed_stop_pct)\n            \n            # 計算止盈 (目標為 BB 中軌)\n            take_profit = row['bb_mid']\n            \n            # 確保風報比合理\n            risk = current_price - stop_loss\n            reward = take_profit - current_price\n            if reward / risk < 1.0:\n                take_profit = current_price + (risk * self.target_rr)\n            \n            return {\n                'signal': 'buy',\n                'stop_loss': stop_loss,\n                'take_profit': take_profit,\n                'entry': current_price,\n                'adx': row['adx'],\n                'rsi': row['rsi'],\n                'reason': f\"超賣反彈: RSI={row['rsi']:.1f}, ADX={row['adx']:.1f}, 成交量萎縮\"\n            }\n        \n        # === 做空條件: 超買回落 ===\n        elif (row['close'] >= row['bb_upper'] and \n              row['rsi'] > self.rsi_overbought and \n              volume_contracted):\n            \n            # 計算止損\n            if self.use_atr_stops:\n                stop_loss = current_price + (atr * self.atr_multiplier)\n            else:\n                stop_loss = current_price * (1 + self.fixed_stop_pct)\n            \n            # 計算止盈\n            take_profit = row['bb_mid']\n            \n            # 確保風報比合理\n            risk = stop_loss - current_price\n            reward = current_price - take_profit\n            if reward / risk < 1.0:\n                take_profit = current_price - (risk * self.target_rr)\n            \n            return {\n                'signal': 'sell',\n                'stop_loss': stop_loss,\n                'take_profit': take_profit,\n                'entry': current_price,\n                'adx': row['adx'],\n                'rsi': row['rsi'],\n                'reason': f\"超買回落: RSI={row['rsi']:.1f}, ADX={row['adx']:.1f}, 成交量萎縮\"\n            }\n        \n        return {'signal': 'hold', 'stop_loss': None, 'take_profit': None, 'reason': '等待設定'}\n    \n    def get_indicators_display(self, df: pd.DataFrame) -> Dict:\n        \"\"\"\u7372取指標顯示資訊\"\"\"\n        df = self.add_indicators(df)\n        row = df.iloc[-1]\n        \n        return {\n            'adx': row['adx'],\n            'rsi': row['rsi'],\n            'bb_upper': row['bb_upper'],\n            'bb_mid': row['bb_mid'],\n            'bb_lower': row['bb_lower'],\n            'bb_width': row['bb_width'],\n            'volume_ratio': row['volume'] / row['volume_ma'] if row['volume_ma'] > 0 else 0,\n            'atr': row['atr']\n        }\n